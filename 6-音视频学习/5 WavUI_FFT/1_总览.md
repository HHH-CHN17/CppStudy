**必读**

- 第3章 ADC and DAC：采样率/奈奎斯特、量化、PCM（代码里就是 int16 PCM + sampleRate）。
- 第8章 The Discrete Fourier Transform：DFT bin、频率分辨率、实信号频谱的基本形态（代码把 FFT 结果映射到频率）。
- 第9章 Applications of the DFT：谱分析的常见坑，尤其是“频谱泄漏/窗函数”的现象与直觉（代码对每帧乘窗）。
- 第10章 Fourier Transform Properties：乘窗为什么会改变频谱（时域相乘↔频域卷积这类关键性质，用来理解窗的副作用）。
- 第14章 Introduction to Digital Filters（只看 dB 那一节）：20*log10、动态范围、噪声底（代码把幅度转 dB 并压到 0..1）。
- 第22章 Audio Processing：人耳听感、为什么常用对数频带/20Hz~20kHz（代码按对数频带聚合能量）。

**选读（理解更稳，但不看也能回到代码）**

- 第2章 Statistics, Probability and Noise：RMS/噪声与“噪声底”的直觉（代码每个频带算 RMS）。
- 第12章 The Fast Fourier Transform：只要知道“FFT 是更快的 DFT、常用 2^N 点、real FFT”即可，不用看算法推导。